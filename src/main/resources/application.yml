spring:
  mvc: 
    static-path-pattern: /static/**
  config:
    import: security-variables.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/thebuild_jinan
    username: root
    password: ${mysql.password}
  thymeleaf:
    cache: false
  application:
    name: TheBuild


mybatis:
  mapper-locations: mappers/*Mapper.xml
